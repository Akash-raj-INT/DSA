#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> v; // This vector is initially empty

    // Insert an element
    v.push_back(10); // 0th element = 10

    // Pop the last element
    v.pop_back(); 

    return 0;
}

